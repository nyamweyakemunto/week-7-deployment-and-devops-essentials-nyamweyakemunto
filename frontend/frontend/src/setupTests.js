// setupTests.js

// Import necessary testing utilities
import '@testing-library/jest-dom/extend-expect';

// Optionally, you may set up global mocks or polyfills here
// For example, mocking CSS modules if needed
jest.mock('./path/to/your/styles.module.css', () => ({}));